# 嵌入式系统与应用 2024
## A卷
### 一 简答 1个6分,48分
芯片级嵌入、模块级嵌入区别
嵌入式系统任意三个特性
如何提高移植性
体系结构演变任写三个变种
分析指令，sub stmfd mrs
大小端 给一个D3~D0在一个地址大小端分别怎么存放
mmu和mmu三个功能
thumb指令分类4个
### 二 分析跳转表 10分
 
### 三 c语言转汇编 15分

### 四 字符串修改，60改85，要求内嵌汇编 15分

### 五 1个3分，12分
实时操作系统特性，软硬实时区别
bootloader
Berkeley risc三个采用的特性
看门狗定时器工作原理

## B卷
2024年1月-嵌入式系统设计与应用-B卷
### 第一题
简述嵌入式计算机。6分
嵌入式系统的运行环境差异很大，具体体现在哪些方面？6分
大端的存储形式。D3D2D1D0四个字节组成的数据，在地址xxxxx中具体怎么存储？
嵌入式系统软件部分的结构层次
Risc架构的三个重要特征。
处理器工作模式有几种？怎么分类？
寄存器的寻址方式中，数据块访问模式有四种，具体是哪四种？他们的后缀有哪些？
### 分析
给出了3条指令。SUBB,CMP,LDM 分析每条指令的意义。有条件码和S。下面是三个指令↓
SUBNES R, R, R
LDM R,｛R～R, R, PC｝
ORREQS, R, R, R
### 代码 10分
字符串函数left，right和cc。给了两个题目，第一个是写出给定字符串的right8，然后第二个是让给定字符串的left和另一个字符串cc起来。写出结果。

用完备的汇编格式，从存储器中复制字符串到另一个存储器地址，目的地址起始位为R2。原起字符串始地址为R3，字符串的长度不定。字符串的最后一位是Null，当在字符串中遇到A时将其替换成B，使用子程序的格式编写。15分

补全代码实现arm状态转换到thumb状态,thumb状态转换到arm状态。

单独编译的c源程序调用单独编译的汇编程序的规则，以及反之规则。
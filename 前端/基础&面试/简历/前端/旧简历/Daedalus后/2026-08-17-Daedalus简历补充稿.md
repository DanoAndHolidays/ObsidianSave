# 2026-08-17-Daedalus简历补充稿
> Last Format Time：9/16/2026 19:24:52

> 用途：删除现有简历中的「一刻短剧 Yike」，并将 Daedalus 项目内容合并进对应实习经历，避免两个栏目重复。
>
> 原 Word 简历未修改。以下正文按一页简历的密度撰写，可直接复制后再调整字号与行距。

---
## 推荐替换后的实习经历（合并版）
核心项目：Daedalus——面向 Coding Agent 与研发团队的 AI 代码审查及代码结构治理平台，通过 Crate、Archetype、Anatomy 等领域模型描述代码组织单元与设计契约。

技术栈：React、TypeScript、Refine、React Query、React Hook Form、Zod、Zustand、Storybook、Turborepo、Bun

- 参与治理元数据 Crate、Archetype、Anatomy、Dictionary 等核心模块开发，覆盖治理元数据的提交、校验、管理等业务链路。

- 重构大量组件

- 针对 500+ 节点仓库文件树，将递归全量渲染改造为“可见节点扁平化 + 虚拟列表”，结合稳定路径 Key 与 overscan，仅挂载视口附近节点，降低首屏 DOM 数量与内存开销。

- 参与 MCP 连接管理链路实现，完成连接创建、详情、复查、撤销、安装引导与 OAuth Consent 页面，通过 Refine → dataProvider → tRPC 组织数据流；

- 重构 Crate 关联路径表单，抽离领域组件并使用 React Hook Form `useFieldArray` 统一数据源，结合共享 Zod Schema 完成跨层校验，解决 stale closure、静默失败及新增/编辑交互割裂问题。












### 前端开发实习生｜Data 大官网｜2026.01—2026.03
- 负责 Quota 配额管理核心模块的白屏化与业务升级，将旧页面拆分为角色管理、审批与编辑子页面，解耦新建/编辑复用表单，并升级通用 Input 组件的自定义布局能力，在兼容历史调用的同时适配空间受限的新视图。
- 面向约 1 万条火山账号数据，引入虚拟滚动并缓存高频计算结果，将页面常驻渲染控制在约 20—30 个节点，缓解大列表滚动卡顿，使页面恢复可用。
- 参与审批、配额申请等关键链路的稳定性治理，负责 Agent 生成代码的逻辑核查、边界测试与冗余组件清理；在 Monorepo 中累计提交 17 个已合并 MR。

---
## 独立项目与开源经历
Daedalus 已完整并入实习经历，本栏目不再重复。删除「一刻短剧 Yike」后，可直接保留原简历中的 Vite DevTools 开源贡献：

### Vite DevTools｜开源贡献｜2025.10—至今
项目简介：用于可视化 Vite/Rolldown 模块图、依赖、Chunk、静态资源与构建元数据的开发工具，可作为嵌入式面板、浏览器扩展或独立网页运行。

- 参与构建产物测试、问题定位与 Bug 修复。
- 为新增功能补充使用文档，降低功能理解与接入成本。

---
## 技术能力同步建议
现有 Word 简历的“框架技术”只突出 Vue，与新增的 Daedalus 经历不一致。建议同步改成：

> 框架技术：熟悉 React、TypeScript 及 React Query、React Hook Form、Zod、Zustand 等生态，具备复杂表单、异步状态、路由与缓存管理实践；熟练使用 Vue 3、Vue Router、Pinia 与 Element Plus。

工程化部分可改成：

> 工程化与质量：熟悉 Vite、Turborepo、Bun/npm、Git、Storybook、单元测试与 Monorepo 协作流程，能够使用 AI 编程工具完成需求拆解、实现验收与边界测试。

---
## 使用前需要确认的字段
- Daedalus 所属公司名称、实习起止时间与地点。
- Daedalus 是否允许公开项目名称、仓库地址和业务描述。
- “两周 6 个已合并 PR”是否可以直接用于对外简历。
- MCP 功能当前是否已经提交、合并或部署；若尚未合并，保留“完成实现 / 本地验证”的措辞，不要改成“上线”。

---
## 证据与措辞边界
以下内容有仓库记录支撑，可用于面试追问准备：

- 两周 6 个已合并 PR、Crate 表单重构、Repository 关联与 500+ 节点文件树虚拟化：[[双周复盘：2026-07-03 至 2026-07-17]]
- MCP 连接管理、OAuth 2.1 权限边界、页面组件、Storybook 与 101 项测试：[[COD-305 安全接入 MCP：实现复盘]]
- Daedalus 的 AI 代码审查平台定位与代码结构元数据模型：[[总结]]、[[引入 Crate]]、[[引入Archetype]]

当前不建议写入简历的结论：

- 不写“COD-305 已生产上线”：记录显示该实现当时尚未提交，真实 MCP Client E2E 与目标数据库部署仍待确认。
- 不写“Coding Agent 已可执行完整扫描与 Finding 处理”：COD-306 现有材料主要是实现计划，不能当作已交付功能。
- 不写“稳定 60 FPS”或“响应时间低于 100 ms”：文件树虚拟化有明确实现，但自动化性能基准仍是待办。
- 不把代达罗斯考试题、参考答案或 Agent 辅助生成内容直接写成个人项目成果；简历只采用已合并 PR、明确实现记录和可观察验证。

---
## 与原简历的对应关系
- 删除原“项目经验”中的「一刻短剧 Yike」整段。
- 将 Daedalus 的项目定位、技术栈和核心成果全部放入对应实习经历，不在“项目经验”中重复。
- 将 Daedalus 实习经历放在 Data 大官网实习之前，按时间倒序排列。
- “Vite DevTools”开源贡献继续保留；如果一页仍放不下，先压缩自我评价和技术能力中的工具枚举，不删减可量化的交付结果。

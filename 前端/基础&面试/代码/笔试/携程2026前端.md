# 携程2026前端
> Last Format Time：9/16/2026 19:24:50

*确实不难666*

ai coding 8641

第二题：找一个算式的和，27%左右的ac，超时了

```ts
const rl = require("readline").createInterface({ input: process.stdin });
var iter = rl[Symbol.asyncIterator]();
const readline = async () => (await iter.next()).value;

void (async function () {
    // Write your code here
    line = await readline();
    let tokens = line.split(" ");
    let n = parseInt(tokens[0]);
    // console.log(n);

    for (let i = 0; i < n; i++) {
        let ans = [];

        line = await readline();
        let tokens = line.split(" ");
        let num = parseInt(tokens[0]);
        // console.log(num);

        line = await readline();
        let arr = line.split(" ");
        // console.log(arr);

        for (let j = 0; j < arr.length; j++) {
            let sum = 0;
            for (let q = 0; q < arr.length; q++) {
                sum += arr[q] * (q % (j + 1));
            }
            ans.push(sum);
        }

        console.log(...ans);
    }
})();
```

第一题：找匹配的（）串的中间的字符的和，100% ac

```ts
const rl = require("readline").createInterface({ input: process.stdin });
var iter = rl[Symbol.asyncIterator]();
const readline = async () => (await iter.next()).value;

void (async function () {
    // Write your code here
    line = await readline();
    let tokens = line.split(" ");
    let n = parseInt(tokens[0]);
    let k = parseInt(tokens[1]);

    let stack = [];
    let nums = [];
    let ans = 0;

    line = await readline();
    let oper = line.split("");

    // console.log(oper);

    for (let i = 0; i < oper.length; i++) {
        // console.log(oper[i], i);
        if (oper[i] === "(") {
            // 记录这个值没有意义
            stack.push({ value: oper[i], index: i });
        } else if (oper[i] === ")") {
            const top = stack.pop();
            nums.push(i - top.index - 1);
        }
    }

    // console.log(nums);
    for (const num of nums) {
        if (num === 0) ans++;
        else if(num % k === 0) ans++;
    }
    console.log(ans)
})();
```
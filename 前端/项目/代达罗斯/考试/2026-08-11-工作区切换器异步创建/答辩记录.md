---
exam_id: EXAM-20260811-01
date: 2026-08-12
type: mastery-verification
score: 8/15
---

# 答辩记录
> Last Format Time：8/13/2026 15:14:03

---
## 第1问：Zustand 浅合并（3/3）
能够说明 `set` 只合并第一层，所以更新 `createName` 不会删除同层的 `menuOpen`；也能判断整个替换嵌套 `form` 时，旧的 `form.error` 不会自动保留。核心结论正确。

---
## 第2问：ResultAsync 值传递（2/4）
能够区分原生 Promise 与 neverthrow 对成功/失败分支的约束，但没有回答去掉 `.map(() => team)` 后成功值会变成 `void`，也没有说明 `andThen` 回调必须返回 `Result`/`ResultAsync`，不能直接返回任意原生 Promise。

---
## 第3问：订阅清理机制（2/3）
已经纠正“`useCallback` 有清理函数”的误解，知道 `removeEventListener` 用于清除监听；但未完整说明 `useSyncExternalStore` 才是调用 subscribe、保存并执行 unsubscribe 的消费者。

---
## 第4问：刷新失败测试（1/5）
写出了 `creating` 和 `createErrMsg` 的状态断言，但缺少失败依赖、动作触发与 `selectWorkspace` mock 断言，测试不能独立运行。

---
## 掌握结论
- Zustand 第一层浅合并：已形成独立理解。
- ResultAsync 组合与成功值变换：引导完成，仍需复测。
- 外部订阅 cleanup：能识别基本职责，调用时序仍需复测。
- mock 副作用验证：掌握缺口，需要练习 `vi.fn()` 与 `toHaveBeenCalled` 系列断言。

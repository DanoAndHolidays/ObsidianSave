# 00 快速入门
> Last Format Time：9/16/2026 19:24:55

持久层（DAO）的库，底层是JDBC

![[Pasted image 20260914222422.png]]

配置文件：
```text
# 这里报错可以换项目的编码格式  
# 配置数据库链接信息  
spring.datasource.url=jdbc:mysql://localhost:3306/db01  
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver  
spring.datasource.username=root  
spring.datasource.password=123456
```

```java
@Mapper // 在运行时会自动的创建代理对象（动态代理），会自动的将对象自动的存入IOC容器中  
public interface UserMapper {  
  
    @Select("select * from user2")  
    public List<User> findAll();  
}
```

```java
@SpringBootTest // //SpringBoot单元测试的注解当前测试类中的测试方法运行时，会启动 springbook 项目 - IOC容器  
class SpringbookWebQuickstartApplicationTests {  
  
    @Autowired  
    private UserMapper userMapper;  
  
    @Test  
    public void testFindAll(){  
        List<User> userList = userMapper.findAll();  
        userList.forEach(System.out::println);  
    }  
}
```

---
## 辅助配置
配置对应的日志打印：
```bash
mybatis.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl  
#==>  Preparing: select * from user2  
#==> Parameters:  
#<==    Columns: id, username, name, age, gender  
#<==        Row: 1, 1, 1, 19, 男  
#<==        Row: 3, 2, 2, 19, 男  
#<==        Row: 96, 2026-09-14 00:09:45, shi, 19, 男  
#<==        Row: 98, 2026-09-14 00:09:20, shi, 19, 男  
#<==        Row: 190, r, shi, 19, 男  
#<==        Row: 196, 2026-09-14 00:10:28, shi, 19, 男  
#<==      Total: 6
```